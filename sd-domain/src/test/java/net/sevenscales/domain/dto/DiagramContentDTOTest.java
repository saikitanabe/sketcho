package net.sevenscales.domain.dto;

import net.sevenscales.domain.DiagramContentDTO;
import net.sevenscales.domain.DiagramItemDTO;
import junit.framework.Assert;
import junit.framework.TestCase;

public class DiagramContentDTOTest extends TestCase {
	public void testConstruction() {
		// DiagramContentDTO dto = new DiagramContentDTO();
		// dto.addItem(new DiagramItemDTO("text", "type", "shape", "background", "textcolor", 2, 1L, "", ""));
		
		// DiagramContentDTO copy = new DiagramContentDTO(dto);
		// copy.getDiagramItems().get(0).setText("text2");
		// Assert.assertNotSame(dto, copy);
//		Assert.assertTrue(!dto.equals(copy));
	}
	
	public void testConstruction2() {
		// DiagramContentDTO dto = new DiagramContentDTO();
		// dto.addItem(new DiagramItemDTO("text", "type", "shape", "background", "textcolor", 2, 1L, "", ""));
		
		// DiagramContentDTO copy = new DiagramContentDTO(dto);
		// copy.getDiagramItems().get(0).setText(null);
		// Assert.assertEquals(dto, copy);
	}

	
}
