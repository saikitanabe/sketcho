<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.artifactId}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>

    <macro name="test2-macro" class="net.sevenscales.confluence.plugins.ExampleMacro" key="my-macro">
    <!-- TODO: Add macro description -->
    <!-- <description></description> -->
    </macro>
    
    <servlet name="Content Image Servlet" 
    		key="contentImageServlet" 
    		class="net.sevenscales.confluence.plugins.test2macro.server.Test2Servlet">
        <description>Content Image Servlet</description>
        <url-pattern>/resources/contentImage</url-pattern>
    </servlet>
    
</atlassian-plugin>
